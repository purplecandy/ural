import 'package:flutter/material.dart';

class ThemedAppBar extends StatelessWidget {
  const ThemedAppBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
